## Problem 1
### 1
**Intersection**

Let $p(x)$ be a polynomial in both $\mathcal{Q}_{n}(1)$ and $\text{span}(1)$.
- Since $p(x) \in \text{span}(1)$, it must be a constant polynomial, so $p(x) = c$ for some real number $c$.
- Since $p(x) \in \mathcal{Q}_{n}(1)$, by definition $p(1) = 0$
- Evaluating the constant polynomial at $x=1$ gives us $c = 0$.
Therefore, $p(x) = 0$, so the intersection is only the zero vector.
    
**Sum**

Let $p(x)$ be any polynomial in $\mathcal{P}_{n}(\mathbb{R})$. We can use the following trick to split it up:

$$p(x) = \underbrace{(p(x) - p(1))}_{\text{term A}} + \underbrace{p(1)}_{\text{term B}}$$
- $p(1)$ is just a number (a constant). Thus, it belongs to $\text{span}(1)$.
- Let $q(x) = p(x) - p(1)$. If we evaluate this at $x=1$, we get $q(1) = p(1) - p(1) = 0$. Since $q(1)=0$, this term belongs to $\mathcal{Q}_{n}(1)$.
Since any polynomial can be written as a sum of something in $\mathcal{Q}_{n}(1)$ and something in $\text{span}(1)$, and their intersection is zero, the direct sum is proven.

### 2
A typical coset consists of all polynomials that have the same value at $x=1$. Because every polynomial $p(x)$ can be represented as "a value at 1" plus "a polynomial that is zero at 1", each coset is uniquely determined by the real number $p(1)$.
### 3
Since $\text{span}(1)$ is generated by a single non-zero vector (the constant function 1), it is 1-dimensional.
## Problem 2

Let $V = \mathbb{R}^2$. Let  $U$ be the y-axis: $U = \text{span}\{(0,1)\}$.

1. Define $V_1$ (The x-axis). Let $V_1 = \text{span}\{(1,0)\}$.
- **Intersection:** The intersection of the x-axis ($V_1$) and y-axis ($U$) is clearly just the origin $\{(0,0)\}$.
- **Sum:** Any vector $(x,y) \in \mathbb{R}^2$ can be written as $(x,0) + (0,y)$, where $(x,0) \in V_1$ and $(0,y) \in U$.
- Therefore, $V = V_1 \oplus U$.

1. Define $V_2$ (The diagonal line $y=x$). Let $V_2 = \text{span}\{(1,1)\}$.
- **Intersection:** A vector in $V_2$ looks like $(c, c)$. For this to be in $U$ (where the x-coordinate is 0), we must have $c=0$. Thus, $V_2 \cap U = \{(0,0)\}$.
- Sum: We need to show any vector $(x,y)$ can be formed by sum of vectors from $V_2$ and $U$.
    We can write $(x,y) = \underbrace{(x,x)}_{\in V_2} + \underbrace{(0, y-x)}_{\in U}$.
- Therefore, $V = V_2 \oplus U$.

We have shown that $V = V_1 \oplus U$ and $V = V_2 \oplus U$.

However, $V_1 \neq V_2$ because $V_1$ contains the vector $(1,0)$ while $V_2$ does not.
## Problem 3
## Problem 4